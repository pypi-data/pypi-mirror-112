from setuptools import setup

setup(name='PyxDistributions',
      version='1.0.0',
      description='Gaussian and Binomial distributions',
      packages=['PyxDistributions'],
      zip_safe=False)
