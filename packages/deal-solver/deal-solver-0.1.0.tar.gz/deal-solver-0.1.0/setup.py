#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['deal_solver',
 'deal_solver._context',
 'deal_solver._funcs',
 'deal_solver._proxies']

package_data = \
{'': ['*']}

install_requires = \
['astroid', 'z3-solver']

extras_require = \
{'lint': ['flake8',
          'flake8-bugbear',
          'flake8-commas',
          'flake8-quotes',
          'isort',
          'mypy>=0.910'],
 'test': ['hypothesis', 'pytest', 'pytest-cov', 'pytest-xdist']}

setup(name='deal-solver',
      version='0.1.0',
      description='z3-powered solver (theorem prover) for deal.',
      author='Gram',
      author_email='gram@orsinium.dev',
      url='https://github.com/life4/deal-solver',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.6',
     )
