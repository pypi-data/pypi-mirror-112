from setuptools import setup

setup(name='SM_New_distributions',
      version='8.1',
      description='Gaussian and Binomial distributions',
      packages=['SM_New_distributions'],
      zip_safe=False)
